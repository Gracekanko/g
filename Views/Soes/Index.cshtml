@model IEnumerable<GogoDriverWeb.Models.So>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Localisation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PersonneIdNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Localisation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonneIdNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
