@model IEnumerable<GogoDriverWeb.Models.TypeVehicule>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Standard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Standard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vip)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TypevehiculeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TypevehiculeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TypevehiculeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
