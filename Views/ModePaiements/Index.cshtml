@model IEnumerable<GogoDriverWeb.Models.ModePaiement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LibelleMode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pourcentage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LibelleMode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pourcentage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModePaiementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModePaiementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModePaiementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
